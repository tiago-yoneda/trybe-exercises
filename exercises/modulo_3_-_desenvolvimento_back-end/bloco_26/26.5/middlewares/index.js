const checkLogin = require('./checkLogin');
const checkToken = require('./checkToken');
const generateToken = require('./generateToken');
const validaOperacao = require('./validaOperacao');
const soma = require('./soma');

module.exports = {
  checkLogin,
  checkToken,
  generateToken,
  validaOperacao,
  soma,
};
